package com.netty.game.model.entity.menu.impl;

import com.netty.game.model.entity.menu.Menu;

/**
 * 
 * @author Joshua Rodrigues
 * @since Sep 25, 2011 2:24:12 PM
 */
public class FriendListMenu extends Menu {

	/**
	 * 
	 */
	public void process() {
		/** int i = class9.contentType;
		if (((i >= 1) && (i <= 200)) || ((i >= 701) && (i <= 900))) {
			if (i >= 801) {
				i -= 701;
			} else if (i >= 701) {
				i -= 601;
			} else if (i >= 101) {
				i -= 101;
			} else {
				i--;
			}
			String name = World.getWorld().getFriendList().get(i).getName();
			this.menuActionName[this.menuActionRow] = "Remove @whi@" + name;
			this.menuActionID[this.menuActionRow] = 792;
			this.menuActionRow++;
			this.menuActionName[this.menuActionRow] = "Message @whi@" + name;
			this.menuActionID[this.menuActionRow] = 639;
			this.menuActionRow++;
			return true;
		}
		if ((i >= 401) && (i <= 500)) {
			this.menuActionName[this.menuActionRow] = "Remove @whi@" + class9.message;
			this.menuActionID[this.menuActionRow] = 322;
			this.menuActionRow++;
			return true;
		}
		return false; */
	}
}
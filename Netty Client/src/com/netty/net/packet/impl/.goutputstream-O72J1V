package com.client.net.packet.impl;

import com.client.game.Client;
import com.client.net.packet.Packet;
import com.client.net.packet.PacketAttribute;
import com.client.net.packet.PacketHandler;

public class InterfaceWalkPacketHandler implements PacketHandler {

	@Override
	public boolean handlePacket(PacketAttribute packetAttribute, Client client, Packet packet) {
		return false;
	}
}